VITE_API_URL=http://localhost:4000/api

# Restrict this key in Google Cloud Console to your actual domains
# (localhost:5173 for dev, ops.goodbyemate.com.au for prod) and to only
# the Maps JavaScript API + Places API before using it anywhere public.
VITE_GOOGLE_MAPS_API_KEY=
