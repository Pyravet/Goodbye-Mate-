// Re-exported from the shared package — this file exists only so every
// existing `./api.js` / `../api.js` import in this app keeps working
// without a mass rewrite. The real implementation lives in
// packages/web-shared/src/api.js; edit it there, not here.
export * from '@goodbye-mate/web-shared/src/api.js';
